FROM python:3.9-slim

WORKDIR /app

# Instalacja YOLOv8 i FastAPI
RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    ultralytics \
    opencv-python-headless

COPY ./python-api /app

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]